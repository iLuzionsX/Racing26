Fix the Muse GitHub Actions patch materialization path so valid model-generated new-file and deleted-file unified diffs do not fail on /dev/null handling. A recent run proposing a new regression file failed with "error: dev/null: No such file or directory" during git apply. Preserve strict git apply checking and never normalize ordinary source lines.

Inspect .github/workflows/muse-agent.yml and scripts/muse-agent parser/guardrail tests. Implement robust metadata-line normalization for diff --git, index, ---/+++, @@ headers, including canonical /dev/null for add/delete patches. Add deterministic parser/materializer tests covering new file, deleted file, modified file, one-leading-space model formatting, and malformed dev/null variants. Keep this infrastructure-only; no physics changes. Ensure the workflow still rejects bad context. Run Muse guardrail tests and any relevant build/test commands.