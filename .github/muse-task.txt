Design and implement persistence for mobile control layout: wheel position + size and pedal-cluster position + size, with versioned localStorage schema, safe parsing/clamping, reset defaults, and sensible portrait/landscape behavior. Must survive reloads and malformed old values. Keep physics untouched.
