You are a second-pass specialist for iLuzionsX/Racing26 on the Showcase Circuit environment-art branch. The validated track geometry, banking, road/runoff widths, surface-provider outputs, spawn, and all M5 physics are LOCKED. Do not edit src/graphics/tracks/showcaseCircuit.ts, showcaseCircuitQA.ts, App.tsx, physics files, package.json, workflows, or validation docs. CREATE NEW MODULE FILES ONLY under src/graphics/tracks/showcase/ (plus public/THIRD_PARTY_ASSETS.txt only for the asset pass). No patch may modify an existing file. Make deterministic, reusable builder APIs for a later integrator. Keep all trackside placement APIs parameterized by barrierOffsetM/path and designed to stay outside the full 18m recovery runoff. Required validation: npm run test:track, npm run test:conventions, npm run test:m5-validation:strict, npm run build. Return a strict standard unified diff.
FOCUS: real CC0 3D asset loader and venue asset compositor. Source is the public GitHub mirror of Kenney Racing Kit (CC0): base URL https://raw.githubusercontent.com/eturner58/game-assets/main/kenney/3D%20assets/Racing%20Kit/Models/GLTF%20format/ . Create kenneyAssetManifest.ts and kenneyVenueAssets.ts, plus public/THIRD_PARTY_ASSETS.txt documenting Kenney Racing Kit CC0 and source URLs. Use GLTFLoader with a cache; load only these small/appropriate models: grandStandCovered.glb, grandStandCoveredRound.glb, pitsGarage.glb, pitsOffice.glb, fenceStraight.glb, billboard.glb, camera_exclusive.glb, tentLong.glb, lightPostModern.glb, flagCheckersSmall.glb, radarEquipment.glb, raceCarGreen.glb, raceCarOrange.glb. NEVER use Kenney road/track meshes because our validated track surface stays authoritative. Loader must fail gracefully and allow procedural scene to continue if remote assets fail. Clone cached scenes safely, normalize scale via measured bounding box, preserve shadows selectively, and provide an async function that places authored asset clusters outside barrier/runoff. No integration into core yet.