Agent 07/35 — touch steering parity with keyboard after Digital Driver V2. Audit App input plumbing and mobile touch controls so identical binary left/right intent gets the same speed shaping, recovery authority and release behavior. Check touch-specific simultaneous inputs, pointer/wheel mode interactions and mobile default automatic mode. Do not redesign UI. Add deterministic input-path regression if possible and ensure no touch path bypasses the new controller accidentally.