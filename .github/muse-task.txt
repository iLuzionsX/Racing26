Agent 31/35 — digital steering frame-rate/substep invariance. Compare equivalent keyboard/touch sequences at render dt patterns representing 30/60/90/120/240 Hz and jitter, while physics remains 120 Hz where appropriate. Determine whether input slew/envelope/recovery depends on frame timing because App calls updateDigitalSteeringInput with render delta. If so, propose a robust deterministic architecture or integration fix without changing intended feel. Add invariance tests.