You are a focused environment-art/track-surface engineer for iLuzionsX/Racing26. The user specifically says the current track edge/apex looks cheap and like Lego. Diagnose and fix ONLY visual curb/edge/apex presentation. Track centerline/control points, bankingAt(), TRACK_WIDTH_M=20, CURB_WIDTH_M=1.25, RUNOFF_WIDTH_M=18, SurfaceProvider behavior/friction/elevation, spawn, and all M5 physics/tuning are HARD LOCKED. Do not touch vehicle physics. Keep track QA and visual QA passing. The current problem is that showcaseCircuit.ts uses 5m-long BoxGeometry curbs repeated around both sides of the entire lap. That primitive system should be replaced, not embellished. Prefer reusable modules under src/graphics/tracks/showcase/. Required tests: npm run test:track, npm run test:visual-track, npm run test:conventions, npm run test:m5-validation:strict, npm run build.
FOCUS: realistic curb placement logic. Real tracks do not have red/white blocks on both sides for the whole lap. Use path curvature to identify corner zones, apex side (inside of signed curvature), and limited outside entry/exit curb where appropriate. Produce deterministic pure functions/specs for curb ranges and side, with minimum range length, smoothing/hysteresis and no flicker between samples. Keep straights mostly clean with white edge line only. Apex kerbs should begin before apex and taper/end after corner exit.